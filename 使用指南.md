# SRT 到插画提示词系统 - 使用指南

## 系统概述

本系统将 SRT 字幕文件自动转换为插画提示词。切分阶段不再分层分段处理，`srt-shot-segmenter` 一次性读取整份 SRT 并直接落盘；生成阶段仍使用独立 Workflow 逐镜头生成插画提示词。

## ✅ 已完成组件

### 1. Agent 定义 (`.claude/agents/`)
- `srt-shot-segmenter.md` - 一次性切分整份 SRT 为镜头的 agent，自行原子落盘到 `result_path`，只回传极小状态对象
- `prompt-generator.md` - 提示词生成 agent，生成后自行原子落盘，只回传极小状态对象
- `prompt-result-verifier.md` - 批量校验 agent，只读校验磁盘上的结果文件，不解读内容质量

### 2. Workflow 脚本 (`.claude/workflows/`)
- ✅ **`generate-illustration-prompts.js`** - 生成插画提示词（逐镜头并发 + 批量校验）

### 3. 技能文件 (`.claude/skills/`)
- `srt-shots/SKILL.md` - 镜头切分技能
- `narrative-illustration-prompt/SKILL.md` - 插画提示词技能

## 🚀 使用方式

### 方法 1：切分 + Workflow 生成（推荐）

#### Step 1: 一次性切分 SRT

直接派发 `srt-shot-segmenter` agent 一次，不再需要预切分脚本或分层 workflow：

```
请调用 srt-shot-segmenter agent：

source_srt: "你的文件.srt 的绝对路径"
result_path: "输出 shots.json 的绝对路径（建议与源 SRT 同目录，如 你的文件目录/shots.json）"
```

agent 会自己读取整份 SRT、执行六步法切分、原子落盘完整结果，只回传：
```json
{
  "status": "success",
  "result_path": ".../shots.json",
  "total_cues": 57,
  "total_shots": 12,
  "error": ""
}
```

完整的 `shots` 数组只存在于 `result_path` 指向的文件里，需要时再读取该文件。

#### Step 2: 运行 Workflow - 生成插画提示词

直接把 Step 1 落盘的 `shots.json` 绝对路径传给 workflow，不需要自己先读文件、手动整理 shots 数组——workflow 内部的 Load 阶段会派生一个 agent 读取该文件并解析出 shots 数组：

```
请运行 Workflow 生成插画提示词：

Workflow 脚本：.claude/workflows/generate-illustration-prompts.js
参数：
- shots_path: "shots.json 的绝对路径"
- style: "扁平、手绘卡通插图，干净清晰的黑色轮廓线"
- protagonist: "标志性的白头卡通人物"
- output_dir: "结果文件要写到哪个绝对路径目录下（prompt-generator 自己写，不是主会话写）"
```

返回结果示例（不含任何 analysis / prompt 正文，只有状态和路径）：
```json
{
  "source_srt": "原字幕文案.srt",
  "style": "...",
  "protagonist": "...",
  "output_dir": "...",
  "total_shots": 7,
  "completed": 7,
  "failed": 0,
  "succeeded_shots": [
    {"shot_id": 1, "shot_time": "00:00:00,100 --> 00:00:03,000", "result_path": ".../shot-0001.prompt.json"}
  ],
  "failed_shots": []
}
```

### 方法 2：对话方式（简单但手动）

直接告诉我：

```
我想将 SRT 文件转换为插画提示词。
SRT 路径：E:\aiCode\claudeTextToPrompt\test\原字幕文案.srt
画风：扁平、手绘卡通插图
主角：标志性的白头卡通人物
```

我会手动执行各步骤。

## 📊 测试记录

使用 `test/原字幕文案.srt`（26 条字幕）验证过一次完整流程，当时是"agent 回传完整正文、主会话手写落盘"的旧协议，成功率 100%。此后架构已变更为"agent 自写落盘 + 批量校验"（见下），旧的测试产物已不代表当前行为，需要重新验证。

## 🎯 切分 Agent 与 Workflow 的职责

### 切分：`srt-shot-segmenter` agent（不再是分层 Workflow）

**输入**:
```json
{
  "source_srt": "原字幕文案.srt 的绝对路径",
  "result_path": "输出 shots.json 的绝对路径"
}
```

**落盘结果**（`result_path` 文件内容）:
```json
{
  "source_srt": "原字幕文案.srt",
  "total_cues": 26,
  "total_shots": 7,
  "shots": [
    {
      "id": 1,
      "start": "00:00:00,100",
      "end": "00:00:03,000",
      "text": "今天体验的人生 副本是长生者的一生",
      "source_srt_ids": [1, 2]
    },
    ...
  ]
}
```

**处理流程**:
1. agent 自己用文件读取工具读取整份 SRT
2. 对全部 cue 一次性执行完整六步法（不再分段，因此不需要边界修复）
3. agent 自己原子落盘完整结果，只回传极小状态对象

### Workflow: `generate-illustration-prompts.js`

**输入**:
```json
{
  "shots_path": "/绝对路径/shots.json",
  "style": "扁平、手绘卡通插图...",
  "protagonist": "标志性的白头卡通人物",
  "output_dir": "/绝对路径/output"
}
```

`shots_path` 是 Step 1 落盘的 `shots.json` 绝对路径，不再需要调用方手动读文件、整理 shots 数组——workflow 的 Load 阶段会自己派生 agent 读取并解析。

**输出**（不含任何 analysis / prompt 正文）:
```json
{
  "source_srt": "原字幕文案.srt",
  "style": "...",
  "protagonist": "...",
  "output_dir": "...",
  "total_shots": 7,
  "completed": 7,
  "failed": 0,
  "succeeded_shots": [
    {"shot_id": 1, "shot_time": "00:00:00,100 --> 00:00:03,000", "result_path": ".../shot-0001.prompt.json"}
  ],
  "failed_shots": [
    {"shot_id": 5, "result_path": ".../shot-0005.prompt.json", "stage": "verify", "error": "self_check.abstract_made_visual is false"}
  ]
}
```

**处理流程**（三个 Phase）:
1. **Load**：workflow 脚本本身没有文件系统权限，读不了 `shots_path`。这一步派生一个 agent 去 Read 该文件，按 schema 只提取 `source_srt` 和每个 shot 的 `id/start/end/text`（丢弃 `source_srt_ids`），结构化返回给 workflow。调用方因此不需要自己先读文件、手动拼 shots 数组再传参——那正是之前"第一次总出错"的根源。
2. **Generate**：workflow 先用一次确定性调用 `mkdir -p output_dir`（已存在也视为成功），确保目录一定存在，不依赖每个并发的 `prompt-generator` 各自判断"目录不存在就创建"这一步——并发数十个 agent 时，靠每个 agent 各自记得执行这个条件判断并不可靠。目录确认后，workflow 为每个 shot 预先算好 `result_path`（`output_dir/shot-NNNN.prompt.json`），并发调用 `prompt-generator`。该 agent 内部执行五步法、十项自检，通过后**自己**用 Bash 原子写入（先写临时文件再替换）完整结果（含 analysis/self_check/prompt），只向 workflow 回传 `{shot_id, status, result_path, error}` 这个极小对象——workflow 从未见过、也不转发正文内容。
3. **Verify**：所有 Generate 完成后，把 `output_dir` 和 `total_shots` 传给 `prompt-result-verifier`，由它调用 `scripts/verify_shots.py` 一次性批量扫描磁盘上的全部结果文件，只对脚本判定失败的条目做人工复核。同样不回传正文。
4. workflow 综合 Generate 阶段状态和 Verify 阶段结果，汇总出 `succeeded_shots`/`failed_shots`，最终 `return` 给主会话。

## 🔑 关键设计决策

### 为什么切分不再用分层 Workflow？

旧方案把大 SRT 按 50 条 cue 切成多个 segment，并行派发多个 `srt-shot-segmenter`，再用 `boundary-fixer` 修复段与段之间的边界，最后纯 JS 合并。这套架构的意义在于避免单次 AI 调用处理内容过多导致质量下降。但实践中一次性交给一个 agent 处理整份 SRT 也能保证质量，还省掉了边界修复这一整个易错环节（重叠 cue 判断、合并去重）。因此现在 `srt-shot-segmenter` 直接读取整份 SRT，一次调用、一次落盘，不再需要预切分脚本、分层 Workflow 和边界修复 agent。

### 为什么让 prompt-generator 自己写文件，而不是主会话写？

如果每个 shot 的完整 analysis/prompt 都通过 `return` 一路传回 workflow 再传回主会话，7 个 shot 就是 7 份完整正文堆进主智能体的上下文——shot 数量一多，主智能体上下文会被大量它根本不需要读的内容占满。`prompt-generator` 本身是一次完整的子智能体会话，天然有工具权限，让它在自己的会话里直接落盘、只回传一个几十字节的状态对象，就把"生成正文"和"主会话上下文"彻底解耦了。主会话自始至终只需要知道"哪些 shot 成功了、文件在哪、谁失败了、为什么"。

### 为什么不设共享的 shots-status.json 状态清单？

每个 shot 的目标路径是确定性命名的（`shot-0001.prompt.json` 等），workflow 一开始就能算出全部预期路径，不需要一份运行时共享文件来"发现"哪些做完了。更重要的是，如果真设一个共享状态文件，会有并发写冲突风险：多个 `prompt-generator` 并行执行时，谁都不该被允许改同一份共享文件，那样最后写的会覆盖前面的记录。所以状态清单是不需要的，Workflow 内存里收集到的 `genResults`/`verifyMap` 已经是完整状态。

### 为什么要单独加一个校验 Phase？

`prompt-generator` 自己报告 `status: "success"` 只代表它*认为*自己写成功了，不代表磁盘上真的有一份结构完整的文件——可能路径权限有问题、可能中途被打断只写了半截。`prompt-result-verifier` 是独立的第二双眼睛，只做只读的结构性核实（文件存在/JSON合法/字段齐全/self_check全true），且是**一次性批量**调用（把当批所有 result_path 打包成一个数组传进去），不是每个 shot 单独校验一次，避免为了校验又多打一堆 LLM 调用。

### 为什么不用文件 IO？（Workflow 脚本本身）

Workflow 脚本本体（`agent()`/`pipeline()` 那层 JS）**没有文件系统访问权限**，只能：
- 通过 `args` 接收数据
- 通过 `return` 返回数据
- 调用 `agent()` 编排 AI 任务（子智能体自身是有工具权限的独立会话）
- 使用纯 JS 处理数据

文件的实际读写发生在 `prompt-generator`（写）、`prompt-result-verifier`（读校验）、Load 阶段派生的读取 agent 各自的会话里，不是 workflow 脚本本体，也不是主会话。

### 为什么 Generate 阶段前要单独确保 output_dir 存在？

`prompt-generator.md` 里写了"如父目录不存在，先创建"，但这只是一条要求每个并发运行的 agent 自己判断执行的软性指令。Generate 阶段一次会并发跑多个 `prompt-generator`，只要有一个疏漏了这个判断步骤直接尝试写入，目录不存在时就会写入失败——这不是并发竞态问题（`mkdir -p` 本身幂等），而是"靠 LLM 每次都记得执行这一步"本身不可靠。所以改为在 Generate 阶段开始前，用一次独立、确定性的 `mkdir -p` 调用统一保证目录存在，不再依赖每个 `prompt-generator` 各自判断。

### 为什么 Workflow 输入是 `shots_path` 而不是 `shots` 数组？

早期设计是调用方（主会话）先读 `shots.json`、手动把 `shots` 数组整理好再传给 Workflow 的 `args.shots`。但主会话本身也不能直接读文件内容再原样转述——一旦 shots 数量较多，手动转述极易出错或遗漏，实测"第一次调用总是出错"。既然 Workflow 内部本来就要用 `agent()` 派生子智能体做实际工作，让 Load 阶段的 agent 直接读 `shots_path` 并结构化输出，比让调用方手动读文件、拼数组更可靠，也符合"文件 IO 都交给有工具权限的 agent 会话"的既有设计原则。唯一的代价是多了一次轻量的读取 agent 调用。

### 参数配置

- **密度目标**: 2-6 秒/shot
- **并发**: 引擎自动控制（不需要手动设置）

## 📁 输出文件

- `shots.json`（或调用时指定的文件名）由 `srt-shot-segmenter` 直接写入 `result_path`，建议与源 SRT 放在同一目录。
- `shot-NNNN.prompt.json` 由 `prompt-generator` 直接写入 `output_dir`（workflow 调用时通过 `args.output_dir` 指定绝对路径）。

## 🎬 图片拼接视频

在 `output_dir` 下已生成全部 `shot-NNNN.png`（或其他后缀）插画图片后，用 `scripts/compose_video.py` 按 `shots.json` 的时间轴把图片拼接成一段无声视频。

### 用法

```bash
python scripts/compose_video.py --dir <项目目录>
```

`<项目目录>` 下需要同时有：
- `shots.json`（`srt-shot-segmenter` 落盘的分镜时间数据）
- `images/`（存放 `shot-0001.png`、`shot-0002.png` ... 等分镜图片，文件名中的四位数字对应 `shots.json` 里的 `id`）

可选参数：
- `--output <路径>`：输出视频路径，默认 `<项目目录>/output.mp4`
- `--fps <数字>`：输出帧率，默认 30

示例：

```bash
python scripts/compose_video.py --dir "文案分镜库/为了薅羊毛，你买了三十个二手手机"
```

### 时间轴规则

每张图片的显示时长 = 下一个分镜的 `start` 减去当前分镜的 `start`（最后一个分镜用 `end - start`）。也就是说分镜之间如果有间隙（没有台词的停顿），会由**上一张图片延续显示**补齐，不会出现黑屏，视频总时长与 `shots.json` 中最后一个分镜的 `end` 完全对齐。

### 依赖

需要系统已安装 `ffmpeg` 并在 PATH 中可用（本项目未打包依赖，依赖用户环境）。脚本内部对每张图片单独用 `-loop 1 -t <时长>` 作为输入源，再用 `filter_complex concat` 拼接（没有用 ffmpeg 的 concat demuxer，因为实测其 `duration` 指令在混合时长场景下会导致总时长跑偏）。

### 尚未支持

- 配音音频轨合成（目前只输出无声视频，如需配音需要另外用 ffmpeg 把音频轨叠加进去）
- 转场特效（目前是硬切）

## 🛠️ 故障排查

### Workflow 失败

**问题**: "execute_bash is not defined"
**原因**: Workflow 脚本不支持文件/bash 操作
**解决**: 已修复，使用纯 JS + args 传参

**问题**: "read is not defined"  
**原因**: 同上
**解决**: 已修复，不再读取文件

### Agent 失败

**问题**: prompt-generator 的 self_check 十项校验未全通过
**原因**: 生成内容不满足技能约束（如背景纯色、主角冲突特征、抽象词泄漏进 prompt 正文）
**解决**: 该 shot 在 Generate 阶段直接标记 `status: "failed"` 且不落盘，Workflow 会继续处理其他 shot，失败原因记录在 `failed_shots`（`stage: "generate"`）

**问题**: prompt-generator 自称成功，但 verifier 判定校验不通过
**原因**: 文件确实写了但结构不完整/自检字段本该是 false 却被虚报为 true/落盘中途出错
**解决**: `prompt-result-verifier` 会在 `failed_shots` 里标出 `stage: "verify"` 及具体原因，这类 shot 需要重新生成

## 📝 下一步扩展

- [ ] 支持批量处理多个 SRT 文件
- [ ] 添加质量评分机制
- [ ] 支持自定义 prompt 模板
- [ ] 导出为其他格式（CSV、Excel）

---

**系统版本**: 5.0（生成 Workflow 新增 Load 阶段，输入改为 `shots_path` 绝对路径，由 workflow 内部 agent 读取解析，调用方不再需要手动读文件拼 shots 数组；校验阶段改为脚本批量扫描 + agent 只读问题项；切分仍为单次全量处理，不再分层/分段/边界修复）
**最后更新**: 2026-09-07
